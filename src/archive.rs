pub mod arch {
    pub fn archive_file(name: &str) {
        println!("Archiving file {}", name);
    }

    pub fn archive_folder(name: &str) {
        println!("Archiving folder {}", name);
    }
}
